# lp-test-links 作業ルール（Claude必読）

このリポジトリは、GROWXの全LP案件のテスト確認用ページをフォルダで集約する場所です。
公開URL：`https://growx-inc.github.io/lp-test-links/{案件フォルダ名}/`

## フォルダ構成

- 案件1件＝ルート直下のフォルダ1つ（例：`kamakura/index.html`）
- フォルダ名は英小文字のローマ字（この名前がそのままURLとログの案件名になる）
- 画像・動画は案件フォルダ内に置く（例: kamakura/media/）。Google Drive等の外部参照は使わず、必要な素材はすべてリポジトリ内で完結させる
- ルートの `index.html`（無地の案内ページ）は削除・変更禁止（目次隠しの役割）

## PRの書き方（修正ログ連携のための必須ルール）

1. PRタイトルは「【{案件フォルダ名}】{内容の要約}」
2. PR本文の冒頭に必ず以下の記録ブロック6項目を書く（項目名は変更禁止、半角コロン＋スペース）
3. 【重要】「プロンプト」の値の先頭に必ず「{案件フォルダ名}｜」を付ける（全角の縦棒）。これがスプレッドシート側で案件を識別する唯一の手がかりになる

記録ブロックの型：

```
工程: {工程}
分類: {分類}
原因: {原因}
重要度: {高/中/低}
公開: {公開前/公開済}
プロンプト: {案件フォルダ名}｜{修正内容や使用プロンプト}
```

## マージの可否（案件ステータス表）

下の表で対象フォルダのステータスを確認してからマージすること。

- 「テスト公開」：新規発行・修正ともClaudeの自動マージ可
- 「公開済み」：PR作成までで止める。マージは人間が行う（勝手にマージしない）

| フォルダ | ステータス |
|---|---|
| testlog | テスト公開（検証用） |
| growx | テスト公開 |

## PRマージ後に必ずやること

1. `.github` リポジトリの `profile/README.md`（案件ポータル）の案件一覧を更新する（新規案件の行追加・ステータス変更）

※ 案件一覧はポータルに一本化済み。このリポジトリのREADMEには一覧を書かない（二重管理の禁止）。
